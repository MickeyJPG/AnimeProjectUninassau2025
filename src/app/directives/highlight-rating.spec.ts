import { HighlightRating } from './highlight-rating';

describe('HighlightRating', () => {
  it('should create an instance', () => {
    const directive = new HighlightRating();
    expect(directive).toBeTruthy();
  });
});
